import Api from '../../utils/request'

export const gouScrollDataList = data => {
  return Api.gouScrollDataList(data)
};

export const maoScrollDataList = data => {
  return Api.maoScrollDataList(data)
};
